<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lotte.users.dao.UserDao">
    <select id="checkuser" parameterType="String" resultType="Integer">
        SELECT count(*)
        FROM users
        WHERE user_email=#{userEmail}
    </select>

    <insert id="signin" parameterType="String">
        INSERT INTO users(user_email)
        VALUES (#{userEmail})
    </insert>

    <select id="getuser" parameterType="String" resultType="com.lotte.users.dto.UserDto">
        SELECT user_no, user_email, user_role, user_grade, user_nickname, user_birth, user_gender, user_address, user_phone, user_valid
        FROM users
        WHERE user_email=#{userEmail}
    </select>

    <select id="checkvalid" parameterType="String" resultType="Integer">
        SELECT user_valid
        FROM users
        WHERE user_email=#{userEmail}
    </select>

    <select id="getuserno" parameterType="String" resultType="Integer">
        SELECT user_no
        FROM users
        WHERE user_email=#{userEmail}
    </select>

    <update id="addprofile" parameterType="com.lotte.users.dto.UserDto">
        UPDATE users
        SET user_nickname = #{userNickname}, user_birth =#{userBirth}, user_gender=#{userGender},  user_address=#{userAddress}, user_phone=#{userPhone}
        WHERE user_no=#{userNo};
    </update>

</mapper>
