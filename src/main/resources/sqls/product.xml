<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lotte.products.dao.ProductDao">
    <insert id="insertProducts" parameterType="com.lotte.products.dto.ProductDto">
        INSERT INTO products(product_name, product_content, product_simple_content, product_price, product_amount, product_stock, product_category)
        VALUES(#{product_name}, #{product_content}, #{product_simple_content}, #{product_price}, #{product_amount}, #{product_stock}, #{product_category})
    </insert>
    <update id="updateProducts" parameterType="com.lotte.products.dto.ProductDto">
        UPDATE products
        SET product_stock=#{product_stock}, product_amount = product_amount+#{product_stock}
        WHERE product_no = #{product_no}
    </update>
</mapper>